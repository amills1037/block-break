# Terraform Configuration Files

Creates AWS resources for website, client and serverless.

## Installation on AWS

Run the Terraform commands to create the AWS resources.

```
terraform init
terraform plan
terraform apply
```

May need to login to AWS from the command line.

```
aws login
```

or if running in a remote container

```
aws login --remote
```

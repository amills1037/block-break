name: Feature branch, server code
on:
  push:
    branches:
      - feature/**
    paths:
      - .github/workflows/**
      - server/**

permissions:
  id-token: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-server:
    name: Test server
    uses: ./.github/workflows/test-maven.yml
    with:
      dir: server

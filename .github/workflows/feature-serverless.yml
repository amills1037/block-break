name: Feature branch, serverless code
on:
  push:
    branches:
      - feature/**
    paths:
      - .github/workflows/**
      - serverless/**

permissions:
  id-token: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-serverless:
    name: Test serverless
    uses: ./.github/workflows/test-maven.yml
    with:
      dir: serverless
